what does the question mean? its saying that given an input string, reverse the order of words and separate it with one space
---------------
how does the code work? you initialize some variables and then scan the input string. while you scan it, you move forward until you find a non-space array (start of a word) if reached the end, then stop. on the second loop, it's the opposite, you disregard the letters and only focus on a space or end of the string. after that you add the substrings of the [i, j] (which is the current word) to the list. once you've done that, you start scanning from the end of your current word. after you do that loop you should have scanned the input string with no extra spaces and accurate words. after that, you reverse the order of words using the STL reverse to reverse vectors in place. once you've done allat, now it's just time to correct the spaces between the reversed words and finally return the result to finish everything.