how does this code work? it's pretty simple, the code loops through both the strings with two pointers (day 6-7 is about two pointers) the first pointer analyzes the first string (t) if it matches with the second string (s). if it does, increment the second pointer. it does this until the loop ends and in the end, it asks a conditional "is the second pointer equal to the second strings length?" and then it returns the answer (yes or no/true or false)